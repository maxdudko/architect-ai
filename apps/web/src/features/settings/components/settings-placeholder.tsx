import { EmptyState } from '@/shared/components';

export function SettingsPlaceholder() {
  return (
    <EmptyState
      title="Personal Settings"
      description="Account-level preferences and integrations are in progress."
    />
  );
}
